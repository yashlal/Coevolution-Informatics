def alg(init_list, gamma):
    t = 0
    while init_list[-1][-1] >= gamme:
        #dostuff
        pass
        t += 1


#handling of values in the big sorted list will be of the form: [index/indices  (joint) probabilities  Information Test Value]
